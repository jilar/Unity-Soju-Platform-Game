using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    /*
    //public Camera cam1;
    //public Camera cam2;
    public GameObject Camera1;
    public Camera Camera2;
    //public GameObject Camera2;

    public bool counterDowndone = false;

    // Use this for initialization
    void Start()
    {
        //Camera cam2 = Camera2.GetComponent<Camera>();
        //cam2 = GameObject.Find("Main Camera").GetComponent<Camera>();
        //cam2.enabled = false;
        //cam1.enabled = false;
        //Camera2 = GameObject.Find("MainCamera2").GetComponent<Camera>();
       // Camera2 = GameObject.Find("MainCamera2");

       // if (Camera2.enabled == true)
        //{
            //Camera2.GetComponent <Camera>().enabled = false;
            //   Camera1.GetComponent<Camera>().enabled = true;
       // }

        StartCoroutine(changeCamera());
    }


    IEnumerator changeCamera()
    {
        while (true)
        {
            yield return new WaitForSeconds(4);
          //  Camera2.enabled = false;
           // if (Camera2.GetComponent<Camera>().enabled == true)
           // {
                //Camera2.GetComponent <Camera>().enabled = false;
                //   Camera1.GetComponent<Camera>().enabled = true;
            //}
        }
    }
    */
}